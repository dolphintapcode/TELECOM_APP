import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFE53935); // Đỏ chủ đạo
  static const Color background = Colors.white;

  // Bạn đang thiếu 2 dòng này:
  static const Color textPrimary = Color(0xFF333333); // Màu đen đậm cho tiêu đề
  static const Color textSecondary = Color(
    0xFF757575,
  ); // Màu xám nhạt cho chữ phụ
}
